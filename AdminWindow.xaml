<Window x:Class="DoAn_QLCSV.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Menu" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="Tag" Value="Active">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                    <Setter Property="Foreground" Value="#4CAF50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="IsExpanded" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="WindowBtnStyle" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="#1E1E1E" CornerRadius="15" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#252526" BorderBrush="#333333" BorderThickness="0,0,1,0" CornerRadius="15,0,0,15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Height="60" Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                                <TextBlock Text="🛡️" FontSize="20"/>
                                <TextBlock Text="ADMIN PANEL" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold" Foreground="White" FontSize="16"/>
                            </StackPanel>
                        </Border>

                        <Button Style="{StaticResource MenuButtonStyle}" Content="🏠   Trang Chủ" Tag="Active" Click="btnDashboard_Click"/>

                        <Expander Header="MAIN MENU">
                            <StackPanel Margin="10,0,0,0">
                                <Button Style="{StaticResource MenuButtonStyle}" Content="👥   Cựu Sinh Viên" Click="btnCuuSV_Click"/>
                                
                                <Button Style="{StaticResource MenuButtonStyle}" Content="📊   Thống Kê" Click="btnThongKe_Click"/>
                                <Button Style="{StaticResource MenuButtonStyle}" Content="📅   Sự Kiện" Click="btnSuKien_Click"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="⚙️   Cài đặt">
                            <StackPanel Margin="10,0,0,0">
                                <Button Style="{StaticResource MenuButtonStyle}" Content="🔒   Đổi mật khẩu" Click="btnChangePass_Click"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="ACCOUNT">
                            <StackPanel Margin="10,0,0,0">
                                <Button Style="{StaticResource MenuButtonStyle}" Content="🚪   Đăng xuất" Foreground="#FF5252" Click="btnLogout_Click"/>
                            </StackPanel>
                        </Expander>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" MouseDown="Header_MouseDown" Background="Transparent" BorderBrush="#333333" BorderThickness="0,0,0,1">
                    <Grid>
                        <TextBlock x:Name="lblTitle" Text="← Trang Chủ" VerticalAlignment="Center" Margin="20,0,0,0" FontWeight="Bold" Foreground="#DDDDDD" Cursor="Hand" FontSize="16"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,10,0">
                            <Button Content="─" Style="{StaticResource WindowBtnStyle}" Click="btnMinimize_Click"/>
                            <Button Content="☐" Style="{StaticResource WindowBtnStyle}" Click="btnMaximize_Click"/>

                            <Button Content="✕" Width="35" Height="30" Click="btnClose_Click">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource WindowBtnStyle}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#D32F2F"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <ContentControl Grid.Row="1" x:Name="MainContent" Margin="20">
                    <Grid>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="👋" FontSize="40" HorizontalAlignment="Center"/>
                            <TextBlock Text="Chào mừng Admin!" FontSize="28" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                            <TextBlock Text="Chọn chức năng từ menu bên trái để bắt đầu." FontSize="14" Foreground="#888" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </ContentControl>
            </Grid>
        </Grid>
    </Border>
</Window>
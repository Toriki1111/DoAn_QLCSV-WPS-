<Window x:Class="DoAn_QLCSV.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Đăng Nhập Hệ Thống" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnLoginStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45a049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Hyperlink">
            <Setter Property="Foreground" Value="#64B5F6"/>
            <Setter Property="TextDecorations" Value="None"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" CornerRadius="20" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" MouseDown="Border_MouseDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20,0,0,20" Background="#6A66EB" ClipToBounds="True">
                <Viewbox Stretch="UniformToFill">
                    <Grid Width="350" Height="500">

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Panel.ZIndex="10">
                            <TextBlock Text="Đề tài IX" FontSize="45" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect ShadowDepth="4" BlurRadius="3" Opacity="0.2"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text="Hệ thống Quản lý cựu sinh viên" FontSize="18" Foreground="#E3F2FD" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>

                        <Canvas VerticalAlignment="Top" Margin="0,50,0,0" Height="20">
                            <TextBlock Text="Thông Báo: WTF" FontSize="18" Foreground="#FFCCBC" FontWeight="Bold" Opacity="0.9" Canvas.Left="-134" Canvas.Top="9" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform x:Name="ThongBao" X="600"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Canvas>

                        <Canvas VerticalAlignment="Top" Margin="0,172,0,0" Height="30">
                            <TextBlock Text="★ CHÀO MỪNG !!! ★       ★ CHÀO MỪNG !!! ★       ★ CHÀO MỪNG !!! ★" FontSize="16" Foreground="#90CAF9" FontWeight="Bold" Opacity="0.7" HorizontalAlignment="Left" Canvas.Left="-347" VerticalAlignment="Center">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform x:Name="TopTextMove" X="-200"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Canvas>

                        <Canvas VerticalAlignment="Top" Margin="0,301,0,0" Height="26">
                            <TextBlock Text="★ CHÀO MỪNG !!! ★       ★ CHÀO MỪNG !!! ★       ★ CHÀO MỪNG !!! ★" FontSize="16" Foreground="#90CAF9" FontWeight="Bold" Opacity="0.7" Canvas.Left="-134" Canvas.Top="9" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock.RenderTransform>
                                    <TranslateTransform x:Name="BottomTextMove" X="800"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Canvas>

                        <Grid.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="ThongBao" Storyboard.TargetProperty="X" From="500" To="-600" Duration="0:0:20" />
                                    </Storyboard>
                                </BeginStoryboard>
                                
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever" BeginTime="0:0:4">
                                        <DoubleAnimation Storyboard.TargetName="TopTextMove" Storyboard.TargetProperty="X" From="-200" To="800" Duration="0:0:10" />
                                        <DoubleAnimation Storyboard.TargetName="BottomTextMove" Storyboard.TargetProperty="X" From="500" To="-500" Duration="0:0:10" />
                                    </Storyboard>
                                </BeginStoryboard>
                                
                            </EventTrigger>
                        </Grid.Triggers>
                    </Grid>
                </Viewbox>
            </Border>

            <Grid Grid.Column="1">

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="100">
                    <Button Content="☐" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="#DDDDDD" FontSize="18" Cursor="Hand" Click="btnMaximize_Click" ToolTip="Phóng to / Thu nhỏ"/>
                    <Button Content="✕" Width="30" Height="30" Margin="5,0,0,0" Background="Transparent" BorderThickness="0" Foreground="#DDDDDD" FontSize="16" Cursor="Hand" Click="btnExit_Click" ToolTip="Thoát"/>
                </StackPanel>

                <StackPanel VerticalAlignment="Center" Margin="40" MaxWidth="400">
                    <TextBlock Text="ĐĂNG NHẬP" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <TextBlock Text="Tên đăng nhập:" Foreground="#BBBBBB" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUsername" Margin="0,0,0,15"/>

                    <TextBlock Text="Mật khẩu:" Foreground="#BBBBBB" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword" Margin="0,0,0,5" PasswordChar="*"/>

                    <TextBlock HorizontalAlignment="Right" Margin="0,0,0,25" FontSize="12">
                <Hyperlink Click="btnForgotPass_Click">Quên mật khẩu?</Hyperlink>
                    </TextBlock>

                    <Button Content="ĐĂNG NHẬP" Style="{StaticResource BtnLoginStyle}" 
                                                Click="btnLogin_Click" 
                                                IsDefault="True"/>          

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <TextBlock Text="Chưa có tài khoản? " Foreground="#BBBBBB"/>
                        <TextBlock>
                    <Hyperlink Click="btnRegister_Click" FontWeight="Bold">Đăng ký ngay</Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
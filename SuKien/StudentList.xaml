<UserControl x:Class="DoAn_QLCSV.StudentList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#BBB"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="BtnFuncStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mã Sinh Viên (Student ID):" Foreground="#FFC107"/>
                    <TextBox x:Name="txtMaSV" IsReadOnly="True" Background="#222" Foreground="#888"/>

                    <TextBlock Text="Họ và Tên (Full Name):"/>
                    <TextBox x:Name="txtHoTen"/>

                    <TextBlock Text="Email:"/>
                    <TextBox x:Name="txtEmail"/>

                    <TextBlock Text="Tiểu sử (Bio):"/>
                    <TextBox x:Name="txtBio" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Khoa (Faculty):"/>
                    <TextBox x:Name="txtFaculty"/>

                    <TextBlock Text="Ngành (Major):"/>
                    <TextBox x:Name="txtNganh"/>

                    <TextBlock Text="Năm Tốt Nghiệp:"/>
                    <TextBox x:Name="txtNamTotNghiep"/>

                    <TextBlock Text="Công ty (Company):"/>
                    <TextBox x:Name="txtCongTy"/>

                    <TextBlock Text="Vị trí (Position):"/>
                    <TextBox x:Name="txtViTri"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Lĩnh vực hoạt động (Industry):"/>
                    <TextBox x:Name="txtIndustry"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Thành phố:"/>
                            <TextBox x:Name="txtCity"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Quốc gia:"/>
                            <TextBox x:Name="txtCountry"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="LinkedIn URL:"/>
                    <TextBox x:Name="txtLinkedIn" Foreground="#64B5F6"/>

                    <TextBlock Text="Facebook URL:"/>
                    <TextBox x:Name="txtFacebook" Foreground="#64B5F6"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="DANH SÁCH CỰU SINH VIÊN" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBox x:Name="txtSearch" Width="300" Height="35" Margin="0" VerticalContentAlignment="Center" TextChanged="txtSearch_TextChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Làm mới" Style="{StaticResource BtnFuncStyle}" Background="#607D8B" Click="btnRefresh_Click"/>
                <Button Content="Thêm Mới" Style="{StaticResource BtnFuncStyle}" Background="#4CAF50" Click="btnAdd_Click"/>
                <Button Content="Lưu Sửa" Style="{StaticResource BtnFuncStyle}" Background="#FF9800" Click="btnUpdate_Click"/>
                <Button Content="Xóa" Style="{StaticResource BtnFuncStyle}" Background="#F44336" Click="btnDelete_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="dgSinhVien" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  Background="#2D2D30" RowBackground="#2D2D30" Foreground="#EEE" BorderBrush="#444"
                  RowHeight="35" SelectionChanged="dgSinhVien_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentId}" Width="100"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding FullName}" Width="200" FontWeight="Bold"/>
                <DataGridTextColumn Header="Ngành" Binding="{Binding MajorName}" Width="150"/>
                <DataGridTextColumn Header="Năm TN" Binding="{Binding GraduationYear}" Width="80"/>
                <DataGridTextColumn Header="Công Ty" Binding="{Binding Company}" Width="150"/>
                <DataGridTextColumn Header="Vị Trí" Binding="{Binding CurrentPosition}" Width="150"/>
                <DataGridTextColumn Header="Thành Phố" Binding="{Binding City}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Tổng số: " Foreground="#AAA"/>
            <TextBlock x:Name="txtTotal" Text="0" Foreground="#4CAF50" FontWeight="Bold" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
<UserControl x:Class="DoAn_QLCSV.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="RowBackground" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#EEE"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="QUẢN LÝ TÀI KHOẢN &amp; PHÂN QUYỀN" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <TextBox Width="300" Height="35" Margin="30,0,0,0" Padding="5" VerticalContentAlignment="Center" 
                     Background="#333" Foreground="White" BorderBrush="#555" Tag="Tìm kiếm user..."/>
            <Button Content="Lưu Thay Đổi" Width="120" Height="35" Margin="15,0,0,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="btnSave_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" x:Name="dgUsers">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding FullName}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="250" IsReadOnly="True"/>

                <!-- Cột Role cho phép sửa -->
                <DataGridTemplateColumn Header="Vai Trò (Role)" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Role, UpdateSourceTrigger=PropertyChanged}" 
                                      Background="#333" Foreground="Black" BorderThickness="0">
                                <ComboBox.ItemsSource>
                                    <x:Array Type="{x:Type System:String}" 
                                             xmlns:System="clr-namespace:System;assembly=mscorlib">
                                        <System:String>admin</System:String>
                                        <System:String>alumni</System:String>
                                        <System:String>pending</System:String>
                                    </x:Array>
                                </ComboBox.ItemsSource>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>

                <DataGridTemplateColumn Header="Hành động" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Reset Mật khẩu" Background="#FF9800" Foreground="White" BorderThickness="0" Padding="10,5" Click="btnResetPass_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
<UserControl x:Class="DoAn_QLCSV.SuKien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#BBB"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="BtnFuncStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mã Sự Kiện (ID - Không sửa):" Foreground="#FFC107"/>
                    <TextBox x:Name="txtEventID" IsReadOnly="True" Background="#222" Foreground="#888"/>

                    <TextBlock Text="Tên Sự Kiện:"/>
                    <TextBox x:Name="txtTenSuKien"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Ngày Bắt Đầu:"/>
                            <DatePicker x:Name="dpStartDate" Height="32" Margin="0,0,0,10" Background="#333"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Thời Gian (VD: 9:00 AM):"/>
                            <TextBox x:Name="txtTime" Text="9:00 AM"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Địa Điểm / Link Online:"/>
                    <TextBox x:Name="txtLocation"/>

                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Mô Tả Chi Tiết Sự Kiện:"/>
                    <TextBox x:Name="txtDescription" Height="190" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="QUẢN LÝ SỰ KIỆN CỰU SINH VIÊN" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Border CornerRadius="4" Background="#333" BorderBrush="#555" BorderThickness="1" Width="300" Height="35">
                    <Grid>
                        <TextBlock Text="🔍 Tìm kiếm sự kiện..." Foreground="#666" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox x:Name="txtSearch" Background="Transparent" BorderThickness="0" Foreground="White" VerticalContentAlignment="Center" Margin="5,0" TextChanged="txtSearch_TextChanged"/>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Làm mới" Style="{StaticResource BtnFuncStyle}" Background="#607D8B" Click="btnRefresh_Click"/>
                <Button Content="Thêm" Style="{StaticResource BtnFuncStyle}" Background="#4CAF50" Click="btnAdd_Click"/>
                <Button Content="💾 Lưu Sửa" Style="{StaticResource BtnFuncStyle}" Background="#FF9800" Click="btnUpdate_Click"/>
                <Button Content="    🗑     " Style="{StaticResource BtnFuncStyle}" Background="#F44336" Click="btnDelete_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="dgEvents" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single"
                  Background="#2D2D30" RowBackground="#2D2D30" Foreground="#EEE" 
                  BorderBrush="#444" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#444"
                  RowHeight="35" HeadersVisibility="Column"
                  SelectionChanged="dgEvents_SelectionChanged"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1E1E1E"/>
                    <Setter Property="Foreground" Value="#AAA"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#444"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#007ACC"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3E3E42"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding EventID}" Width="70"/>
                <DataGridTextColumn Header="Tên Sự Kiện" Binding="{Binding Name}" Width="2.5*" FontWeight="Bold"/>
                <DataGridTextColumn Header="Ngày Bắt Đầu" Binding="{Binding StartDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Địa Điểm" Binding="{Binding Location}" Width="2*"/>
                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Lượt Đăng Ký" Binding="{Binding RegisteredCount}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Tổng số sự kiện: " Foreground="#AAA" FontSize="14" VerticalAlignment="Center"/>
            <TextBlock x:Name="txtTotal" Text="0" Foreground="#4CAF50" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>